[hover add (@param/@return doc)]
```rosegold
add: fn(Int, Int) -> Int
```

---
Adds two numbers together.

- `a` — the first addend
- `b` — the second addend
- **returns** — their sum
----
[hover add — from a call site]
```rosegold
add: fn(Int, Int) -> Int
```

---
Adds two numbers together.

- `a` — the first addend
- `b` — the second addend
- **returns** — their sum
----
[hover method with a doc]
```rosegold
bump: fn() -> Int
```

---
Increment the counter, clamping at `cap`.

- **returns** — the new count
----
[hover xlib.square across files] doc present = True
[exit] 0
